<%- include('partials/header') %>
<main>
  <h1><%= title %></h1>
  <% if (typeof errors !== 'undefined' && errors.length > 0) { %>
    <div class="error-summary">
      <h2>Please correct the following errors:</h2>
      <ul>
        <% errors.forEach(error => { %>
          <li><%= error.msg %></li>
        <% }); %>
      </ul>
    </div>
  <% } %>
  <form method="POST" action="/new-project">
    <div>
      <label for="title">Project Title:</label>
      <input
        type="text"
        id="title"
        name="title"
        value="<%= typeof formData !== 'undefined' ? formData.title || '' : '' %>"
        required
      />
    </div>
    <div>
      <label for="description">Description:</label>
      <textarea id="description" name="description" required><%= typeof formData !== 'undefined' ? formData.description || '' : '' %></textarea>
    </div>
    <div>
      <label for="location">Location:</label>
      <input
        type="text"
        id="location"
        name="location"
        value="<%= typeof formData !== 'undefined' ? formData.location || '' : '' %>"
        required
      />
    </div>
    <div>
      <label for="projectDate">Project Date:</label>
      <input
        type="date"
        id="projectDate"
        name="projectDate"
        value="<%= typeof formData !== 'undefined' ? formData.projectDate || '' : '' %>"
        required
      />
    </div>
    <div>
      <label for="organizationId">Organization:</label>
      <select id="organizationId" name="organizationId" required>
        <option value="">-- Select an Organization --</option>
        <% organizations.forEach(org => { %>
          <option
            value="<%= org.organization_id %>"
            <%= typeof formData !== 'undefined' && formData.organizationId == org.organization_id ? 'selected' : '' %>
          >
            <%= org.name %>
          </option>
        <% }); %>
      </select>
    </div>
    <button type="submit">Create Project</button>
  </form>
</main>
<%- include('partials/footer') %>
